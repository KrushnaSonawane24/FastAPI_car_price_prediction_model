services:
  - type: web
    name: fastapi-car-price-api
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn car_price_api.app.main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PYTHON_VERSION
        value: 3.13.4
